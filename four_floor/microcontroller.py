"""
DEPRECATED — superseded by main.py + monitor.py + sensor.py.

This was an early prototype (simultaneous 4-sensor mock-up, broken relative
import, unimplemented set_led). It's no longer used anywhere in the
pipeline. Safe to delete:

    rm four_floor/microcontroller.py

Kept only as a placeholder because this environment couldn't delete the
file directly — your Terminal can.
"""
