# SUPERSEDED scratch note. Kept for the record only.
# The current entry points are documented in the root README.md.
# One path was redacted; ten_floor/ is not part of this repository.

Four Floor
source ~/.venvs/ucl/bin/activate
python -m simulation.matrices
python -m simulation.damping 
python -m simulation.excitation.py 
python -m simulation.validation.py (fixed)
python -m train.py (fixed)
evaluate.py / building_dashboard.py (fixed) 

ten Floor
python -m ten_floor.simulation.generate_data_gen
python -m ten_floor.simulation.train_gen
python -m ten_floor.simulation.validation.gen
python -m ten_floor.moehle_eval --data_dir <path-to>/ten_floor/Moehle